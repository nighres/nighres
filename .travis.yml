matrix:
    include:
        - language: java
          before_install:
              - sudo apt-get -qq update
              - sudo apt-get -y install jcc
              - pip install --upgrade wheel
          script:
              ./build.sh
